$(".lesson-content").html("<%= j render 'exams/result', questions: @lesson.questions %>")
var old_element = $(".lessons-list .card-lesson-show").get(<%= @user_course.lesson_step - 1 %>);
new_element = $(old_element).replaceWith("<%= j render 'lessons/show_lesson'%>");
