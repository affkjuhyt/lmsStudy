<%= link_to courses_path(@course), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
  <%= notification.actor.first_name %>
  <%= notification.action %>
  <%= notification.notifiable_type.underscore.humanize.downcase %>
<% end %>
